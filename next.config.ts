import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "sjc.microlink.io", "s.udemycdn.com", "cms-images.udemycdn.com", "cdn0.desidime.com" ],
  },
};

export default nextConfig;
